<{{ html | default: 'div' }} class="tracking-tight leading-none relative font-bold font-narrow capitalize text-lg">
	<span class="leading-none block">{{ title | default: 'Add a title' }}</span>
</{{ html | default: 'div' }}>
